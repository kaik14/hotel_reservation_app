import 'package:flutter/material.dart';

class StaffRoomPage extends StatelessWidget {
  const StaffRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: SizedBox.expand());
  }
}
